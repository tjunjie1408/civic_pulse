# README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a polished, source-backed README that serves Hackathon reviewers first and developers second.

**Architecture:** The README uses a two-layer narrative: product value and demo story first, then architecture, commands, verification, and limitations. All claims are grounded in the current repository; absent features such as a production API launcher and a license are called out explicitly instead of implied.

**Tech Stack:** Markdown, Mermaid, Python 3.12, uv, FastAPI, Streamlit, pytest, Pyright, Ruff.

## Global Constraints

- The repository currently has no `README.md` and no license file.
- The Streamlit entrypoint is `src/civicpulse_dashboard/app.py`.
- The FastAPI boundary exposes `create_app()` but the repository has no current default service launcher; do not document a nonexistent `create_default_app`.
- The deterministic demo seed contains 120 complaints in five synthetic Shah Alam-inspired zones.
- The README must distinguish confirmed incidents, review-required evidence, and conflict incidents.
- All demo geography and complaints must be labeled synthetic.
- Do not include secrets, generated databases, model caches, or untracked tool metadata.

---

### Task 1: Create the README

**Files:**
- Create: `README.md`
- Read: `src/civicpulse/demo_seed.py`, `src/civicpulse/api/app.py`, `src/civicpulse_dashboard/app.py`, `scripts/prewarm_model.py`, `pyproject.toml`

**Interfaces:**
- Produces the repository's public project overview and local runbook.

- [ ] **Step 1: Write the hero, product story, and demo geography**

Use the verified project name, the offline-first civic incident framing, the review/priority uncertainty boundary, and these exact zone names:

```text
Taman Seri Murni
Seksyen Harmoni
Kawasan Perindustrian Maju
Pusat Komersial Sentosa
Kampung Sungai Damai
```

- [ ] **Step 2: Add architecture, API boundary, and workflow sections**

Show the Streamlit → typed HTTP gateway → FastAPI → application service → SQLite/read-model flow. State that the Dashboard does not import backend service, repository, SQLite, or domain internals.

- [ ] **Step 3: Add truthful quick-start and verification commands**

Document `uv sync --offline`, `uv run --offline python -m scripts.prewarm_model`, and `uv run --offline streamlit run src/civicpulse_dashboard/app.py`. Explain that a complete API runtime launcher is not currently included and the Dashboard expects `CIVICPULSE_API_URL` to point at an API instance.

- [ ] **Step 4: Add repository map, API surface, limitations, and roadmap**

Mention the frozen OpenAPI v1 snapshot, deterministic reset safeguards, no license file, synthetic data, no authentication framework, and Phase 9 reliability/performance next steps.

- [ ] **Step 5: Verify the Markdown and source claims**

Run:

```powershell
rg -n "TODO|TBD|create_default_app|LICENSE|Taman Seri Murni|180 passed|prewarm_model|src/civicpulse_dashboard/app.py" README.md
git diff --check -- README.md
```

Expected: no placeholder text, no nonexistent launcher claim, all five zones present, and no whitespace errors.

### Task 2: Final review

- [ ] **Step 1: Read the rendered structure**

Check that the first screenful explains what CivicPulse does, why the uncertainty boundary matters, and what the demo geography represents.

- [ ] **Step 2: Confirm no unrelated files are changed**

Run `git status --short` and verify only `README.md` is modified by this task; do not stage or commit unless explicitly requested.
